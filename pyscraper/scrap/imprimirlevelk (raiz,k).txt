len_levelk (raiz,k)
si ( raiz == nulo)
retorne -1
fin si
si raiz <> nulo
 q<-[]
 encolar(q,top+1,base,raiz)
 c_nivel=0
 Mq(top<> base) haga
 si nivel==k
 arr<-[]
 j<-0
 para i =1,q.len,1 haga
 j<-j+1
 arr[j]<- q[i]
 fin para
retorne arr.len()
sino
t<-dequeue(top,base+1,len,q[top])

 para i =1,t.hijos.len,1 haga
 encolar(top+1,base,len , t.hijos[i])
 fin para
fin si
c_level=c_level+1
fin Mq
retorne:
fin si
fin funcion
# ya obtenido los nodos al nivel k solo uso  el metodo len para obtener la anchura del nivel.

max_anchura(raiz, h)

si (raiz <> nulo) ent

 max<-0
  
para (nivel=0, h,+1 ) haga
l = len_levelk(raiz, nivel)
si l > max ent
max <- l
sino
continue
fin si
fin para
retorne max
fin funcion








